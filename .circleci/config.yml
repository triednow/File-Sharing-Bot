# This config was automatically generated from your source code
# Stacks detected: cicd:github-actions:.github/workflows,deps:python:.
version: 2.1
orbs:
  python: circleci/python@2
jobs:
  deploy:
    # Install dependencies and run tests
    docker:
      - image: admin44449999/file
    steps:
      - checkout
      - python/install-packages
      - run:
          name: Run tests
          command: apt install git && git clone https://github.com/aryanchy449/File-Sharing-Bot/ && cd File-Sharing-Bot && bash start.sh
          
          
          
  deploy2:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: admin44449999/mtl
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: docker run -p 8080:8080 admin44449999/mtl
      
workflows:
  build-and-test:
    jobs:
      - deploy
   #   - deploy:
    #     requires:
    #       - test-python
